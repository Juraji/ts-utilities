/**
 * @module SimpleLogger
 */
/** */

/**
 * Logging level presets.
 * Each level will have all lower levels enabled as well.
 */
export enum Level {
    OFF = 0,
    ERROR = 1,
    WARN = 2,
    INFO = 3,
    DEBUG = 4
}
